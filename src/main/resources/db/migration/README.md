# Flyway migrations

This directory intentionally contains no migration yet. The current schema is documented as manually created and has not been verified against a live database. Add only forward, immutable migrations here after that verification.
